# My Car Tracker — اپ ارسال موقعیت مکانی از طریق پیامک

## این اپ چه کاری انجام می‌دهد
1. اگر از یکی از **شماره‌های مجاز (لیست سفید)** که خودتان در تنظیمات وارد کرده‌اید، پیامکی با متن دقیق `sendloc` دریافت شود، اپ مختصات فعلی گوشی را در قالب پیامک (به همراه لینک گوگل‌مپ) به همان شماره پاسخ می‌دهد. پیامک `sendloc` از هر شماره‌ی دیگری نادیده گرفته می‌شود.
2. اگر «ارسال خودکار» در تنظیمات فعال باشد، اپ هر بازه زمانی که مشخص کرده‌اید (حداقل ۱۵ دقیقه، به دلیل محدودیت خود سیستم‌عامل اندروید در WorkManager) مختصات را به‌صورت خودکار به شماره‌ی ذخیره‌شده پیامک می‌کند.
3. باز کردن خود برنامه با **رمز عبور** محافظت می‌شود. بار اول که برنامه را باز می‌کنید، از شما یک رمز عبور خواسته می‌شود (حداقل ۴ کاراکتر)؛ دفعات بعد باید همان رمز را وارد کنید. با هر بار رفتن برنامه به پس‌زمینه (خروج از اپ یا رفتن به Recents)، دفعه بعد که برمی‌گردید دوباره رمز خواسته می‌شود.
4. **کنترل ارسال خودکار از راه دور با پیامک:** از یکی از شماره‌های مجاز می‌توانید پیامک `autosend on 30` بفرستید تا ارسال خودکار هر ۳۰ دقیقه (یا هر عددی که بجای ۳۰ بگذارید، حداقل ۱۵) فعال شود، یا پیامک `autosend off` بفرستید تا غیرفعال شود. اپ یک پیامک تایید کوتاه به همان شماره برمی‌گرداند.
5. **ذخیره‌ی مختصات در فایل، با تایمر مستقل:** یک تایمر کاملاً جدا از ارسال خودکار پیامکی وجود دارد که همیشه فعال است — چه ارسال خودکار پیامکی روشن باشد چه خاموش. با بازه‌ای که خودتان تعیین می‌کنید، مختصات همراه با تاریخ و ساعت در یک فایل CSV روی خود گوشی ذخیره می‌شود.
6. **چرخش ماهانه‌ی فایل لاگ:** برای جلوگیری از حجیم شدن فایل، هر حدود ۳۰ روز فایل لاگ فعلی به‌صورت خودکار بسته و آرشیو می‌شود و یک فایل جدید و خالی باز می‌شود.
7. **ارسال فایل‌های لاگ به ایمیل با پیامک `sendlog`:** از یکی از شماره‌های مجاز پیامک `sendlog` بفرستید تا (در صورت وجود اتصال اینترنت روی گوشی) فایل لاگ فعلی و تمام آرشیوهای قبلی به‌عنوان پیوست به ایمیلی که در تنظیمات وارد کرده‌اید فرستاده شود. اگر اینترنت وصل نباشد یا تنظیمات ایمیل ناقص باشد، یک پیامک توضیح‌دهنده برمی‌گردد.
8. **حذف آرشیوهای قدیمی با پیامک `dellog`:** از یکی از شماره‌های مجاز پیامک `dellog` بفرستید تا فایل‌های آرشیوشده (نه فایل فعلی) پاک شوند و یک پیامک تایید با تعداد فایل‌های حذف‌شده برگردد.

## ⚠️ نکته حقوقی و اخلاقی مهم
این اپ را فقط روی گوشی خودتان یا گوشی افرادی نصب کنید که **کاملاً از وجود آن مطلع‌اند و رضایت داده‌اند** (مثلاً فرزند خردسال با اطلاع و توافق والدین، یا عضوی از خانواده که خودش درخواست کرده). ردیابی مخفیانه‌ی موقعیت مکانی شخص دیگر بدون اطلاع او در بسیاری از کشورها جرم است.

همچنین به دلیل مجوزهای SMS و موقعیت مکانیِ پس‌زمینه، این اپ به شکل فعلی احتمالاً در گوگل‌پلی تایید نمی‌شود و باید به‌صورت APK مستقیم (نصب دستی / sideload) روی گوشی نصب شود.

## ساختار پروژه
```
LocationSmsApp/
├── build.gradle, settings.gradle       ← فایل‌های تنظیمات گریدل
└── app/
    ├── build.gradle                     ← وابستگی‌ها (WorkManager، Play Services Location، JavaMail)
    └── src/main/
        ├── AndroidManifest.xml
        ├── java/ir/example/locationsms/
        │   ├── LoginActivity.kt          ← صفحه ورود با رمز عبور (صفحه اول برنامه)
        │   ├── MainActivity.kt           ← صفحه تنظیمات و درخواست مجوزها
        │   ├── SmsReceiver.kt            ← دریافت پیامک ورودی، بررسی لیست سفید و تشخیص دستورات
        │   ├── ReplyLocationWorker.kt    ← پاسخ فوری با موقعیت مکانی (sendloc)
        │   ├── ReplyTextWorker.kt        ← ارسال پیامک تاییدیه برای autosend/dellog
        │   ├── AutoLocationWorker.kt     ← ارسال دوره‌ای خودکار موقعیت با پیامک
        │   ├── LocationLogWorker.kt      ← ذخیره‌ی دوره‌ای مختصات در فایل (تایمر مستقل)
        │   ├── SendLogEmailWorker.kt     ← بررسی اینترنت و ارسال فایل‌های لاگ به ایمیل (sendlog)
        │   ├── EmailSender.kt            ← ارسال ایمیل با SMTP (JavaMail)
        │   ├── LocationHelper.kt         ← دریافت مختصات GPS
        │   ├── LocationLogger.kt         ← نوشتن/چرخش/آرشیو فایل CSV محلی
        │   ├── SettingsRepository.kt     ← ذخیره تنظیمات (SharedPreferences)
        │   ├── WorkScheduler.kt          ← زمان‌بندی ارسال خودکار پیامکی با WorkManager
        │   ├── LogWorkScheduler.kt       ← زمان‌بندی مستقل ذخیره در فایل با WorkManager
        │   ├── PhoneUtils.kt             ← نرمال‌سازی و تطبیق شماره تلفن برای لیست سفید
        │   ├── PasswordUtils.kt          ← هش کردن رمز عبور با salt (SHA-256)
        │   └── AppLockState.kt           ← وضعیت قفل/باز بودن برنامه در حافظه
        └── res/
            ├── layout/activity_main.xml
            ├── layout/activity_login.xml
            └── values/strings.xml
```

## ساخت فایل APK
من در این محیط به اینترنت و Android SDK دسترسی ندارم، پس نمی‌توانم خودم فایل APK نهایی را کامپایل کنم — فقط کد سورس را می‌دهم. دو راه برای گرفتن APK دارید:

### روش ۱: بدون نصب چیزی، با GitHub Actions (ساده‌ترین راه)
1. یک ریپازیتوری رایگان در GitHub بسازید و کل پوشه `LocationSmsApp` (شامل فایل `.github/workflows/build-apk.yml` که همراهش گذاشته‌ام) را در آن push کنید.
2. به تب **Actions** در ریپازیتوری بروید؛ workflow به‌نام «Build APK» به‌صورت خودکار اجرا می‌شود (یا خودتان با «Run workflow» اجرایش کنید).
3. بعد از اتمام (چند دقیقه)، در پایین صفحه اجرا، بخش **Artifacts** یک فایل به‌نام `LocationSmsApp-debug-apk` می‌گذارد؛ آن را دانلود کنید — همان APK نصب‌شدنی است.
4. این APK یک نسخه‌ی *debug* است که برای نصب و تست شخصی کاملاً کافی است.

### روش ۲: با Android Studio (کنترل بیشتر، امکان نسخه release امضاشده)
همان مراحل بخش «نحوه ساخت پروژه» در ادامه همین فایل. از منوی **Build → Build App Bundle(s) / APK(s) → Build APK(s)** فایل APK را می‌سازید و مسیرش را Android Studio نشانتان می‌دهد.

## سازگاری با نسخه‌های اندروید
`minSdk` پروژه را روی **21 (اندروید 5.0 Lollipop، سال 2014)** تنظیم کرده‌ام که عملاً یعنی روی تقریباً همه‌ی گوشی‌های واقعی امروز نصب و اجرا می‌شود؛ رفتن پایین‌تر از این عملی نیست چون خود کتابخانه‌های AndroidX و Google Play Services حداقل API 21 را می‌خواهند. کد به‌گونه‌ای نوشته شده که با بررسی نسخه‌ی اندروید (`Build.VERSION.SDK_INT`) در گوشی‌های قدیمی‌تر و جدیدتر هر دو درست کار می‌کند (مثلاً درخواست مجوز موقعیت مکانی پس‌زمینه فقط در اندروید ۱۰ به بعد نمایش داده می‌شود چون در نسخه‌های قدیمی‌تر اصلاً چنین مجوزی وجود ندارد).

## نحوه ساخت (Build) پروژه
1. Android Studio را باز کنید → **New Project → Empty Views Activity** → زبان Kotlin.
   - Package name را دقیقاً `ir.example.locationsms` بگذارید (یا اگر عوض کردید، در تمام فایل‌های `.kt` و `AndroidManifest.xml` هم عوضش کنید).
   - Minimum SDK را روی API 26 (Android 8) بگذارید.
2. بعد از ساخته‌شدن پروژه، فایل‌های زیر را با فایل‌های همین پوشه جایگزین/اضافه کنید:
   - `app/build.gradle`
   - `app/src/main/AndroidManifest.xml`
   - `app/src/main/res/layout/activity_main.xml`
   - `app/src/main/res/values/strings.xml`
   - تمام فایل‌های داخل `app/src/main/java/ir/example/locationsms/`
3. روی «Sync Now» بزنید تا وابستگی‌ها (WorkManager و Play Services Location) دانلود شوند.
4. اپ را روی گوشی یا شبیه‌ساز اجرا (Run) کنید.

## نحوه استفاده در اپ
1. اپ را باز کنید. بار اول یک رمز عبور برای ورود به برنامه تعیین می‌کنید (این رمز جدا از رمز قفل گوشی است و فقط برای باز کردن همین اپ استفاده می‌شود).
2. در صفحه تنظیمات، شماره‌های مجاز برای درخواست `sendloc` را وارد کنید (با کاما جدا کنید، مثل `09121234567, 09123334455`). فقط پیامک از این شماره‌ها پاسخ داده می‌شود.
3. شماره‌ای که می‌خواهید موقعیت مکانی به‌صورت خودکار برایش ارسال شود را وارد کنید.
4. بازه زمانی ارسال خودکار (به دقیقه، حداقل ۱۵) را وارد کنید — هر عددی که بخواهید می‌توانید بدهید و هر وقت بخواهید می‌توانید آن را تغییر دهید.
5. کلید «فعال‌سازی ارسال خودکار» را روشن کنید (اگر فقط پاسخ به sendloc کافی است، لازم نیست روشنش کنید).
6. روی «ذخیره و درخواست مجوزها» بزنید و تمام مجوزهای درخواستی (پیامک، موقعیت مکانی، و برای اندروید ۱۰ به بعد گزینه‌ی «Allow all the time» برای موقعیت مکانی) را تایید کنید.

### درباره رمز عبور ورود به برنامه
- رمز با نمک تصادفی (salt) و الگوریتم SHA-256 هش شده و در SharedPreferences ذخیره می‌شود؛ خود رمز اصلی جایی ذخیره نمی‌شود.
- در این نسخه گزینه‌ی «فراموشی رمز» یا «تغییر رمز» از داخل اپ وجود ندارد. اگر رمز را فراموش کردید، ساده‌ترین راه پاک کردن Data/Storage اپ از تنظیمات گوشی است (توجه: این کار تنظیمات ذخیره‌شده شامل شماره‌ها و بازه زمانی را هم پاک می‌کند).

## نکات فنی مهم
- **محدودیت ۱۵ دقیقه‌ای:** این محدودیت خود اندروید (WorkManager) است و برای صرفه‌جویی در باتری وضع شده؛ نمی‌توان آن را دور زد مگر با راهکارهای پرمصرف‌تر مثل Foreground Service دائمی.
- **بهینه‌سازی باتری:** در گوشی‌های شیائومی، هواوی، سامسونگ و... حتماً از تنظیمات گوشی، بهینه‌سازی باتری (Battery Optimization) را برای این اپ غیرفعال کنید، وگرنه سیستم‌عامل اپ را در پس‌زمینه می‌کشد و کار دوره‌ای اجرا نمی‌شود.
- **مجوز موقعیت مکانی پس‌زمینه:** در اندروید ۱۰ به بعد، برای دریافت مختصات وقتی اپ باز نیست، باید گزینه «Allow all the time» را از تنظیمات سیستم انتخاب کنید (خود اپ شما را به آن صفحه هدایت می‌کند).
- **اپ پیش‌فرض پیامک نیست:** این اپ نیازی نیست اپ پیش‌فرض SMS گوشی باشد؛ فقط با مجوز RECEIVE_SMS پیامک‌های ورودی را می‌بیند و با SEND_SMS پاسخ می‌فرستد.
- **دقت مکان:** ابتدا سعی می‌شود یک موقعیت تازه با دقت بالا گرفته شود؛ اگر گرفتن موقعیت تازه ممکن نبود (مثلاً داخل ساختمان)، آخرین موقعیت شناخته‌شده ارسال می‌شود.

## کنترل از راه دور با پیامک
از یکی از شماره‌های مجاز (همان لیست سفیدی که در تنظیمات وارد کرده‌اید)، این دستورات پذیرفته می‌شوند:

| پیامک | نتیجه |
|---|---|
| `sendloc` | مختصات فعلی فوراً به همان شماره پیامک می‌شود |
| `autosend on 30` | ارسال خودکار پیامکی هر ۳۰ دقیقه فعال می‌شود (به‌جای ۳۰ هر عددی بگذارید؛ حداقل ۱۵ در نظر گرفته می‌شود) |
| `autosend off` | ارسال خودکار پیامکی غیرفعال می‌شود (ذخیره در فایل همچنان با تایمر مستقل خودش ادامه دارد) |
| `sendlog` | فایل لاگ فعلی و آرشیوهای قبلی به ایمیل تنظیم‌شده ارسال می‌شوند (نیاز به اینترنت) |
| `dellog` | فایل‌های آرشیوشده‌ی قدیمی حذف می‌شوند (فایل فعلی دست‌نخورده می‌ماند) |

بعد از هر دستور، اپ یک پیامک تایید کوتاه به همان شماره برمی‌گرداند (مثلاً «ارسال خودکار موقعیت هر ۳۰ دقیقه فعال شد.»، «فایل لاگ با موفقیت به ایمیل ارسال شد.»، یا «۲ فایل لاگ آرشیو‌شده حذف شد.»). دستورات `autosend on/off` دقیقاً همان تنظیماتی را که در صفحه‌ی اصلی اپ می‌بینید تغییر می‌دهند، پس اگر بعداً وارد اپ شوید، تغییرات را در صفحه تنظیمات هم می‌بینید.

## ذخیره‌ی مختصات در فایل — تایمر مستقل
برخلاف ارسال خودکار پیامکی (که با سوییچ اپ یا دستور `autosend on/off` روشن/خاموش می‌شود)، **ذخیره در فایل همیشه فعال است** و تایمر جدا و مستقل خودش را دارد (فیلد «بازه ذخیره در فایل» در تنظیمات). یعنی حتی اگر ارسال خودکار پیامکی را خاموش کنید، مختصات همچنان طبق بازه‌ای که تعیین کرده‌اید در فایل ذخیره می‌شود.

هر بار که این تایمر اجرا می‌شود، یک ردیف جدید شامل تاریخ/ساعت و مختصات به فایل فعال اضافه می‌شود:

```
Android/data/ir.example.locationsms/files/location_log.csv
```

فرمت فایل ساده و CSV است:

```
timestamp,latitude,longitude
2026-07-13 14:30:00,35.715298,51.404343
2026-07-13 15:00:00,35.715310,51.404350
```

### چرخش ماهانه (Log Rotation)
برای جلوگیری از حجیم‌شدن فایل، تقریباً هر ۳۰ روز از باز شدن فایل فعلی، همان فایل بسته و به یک نام آرشیو با تاریخ/ساعت تغییر نام داده می‌شود (مثلاً `location_log_archive_20260813_143000.csv`) و یک فایل تازه‌ی `location_log.csv` برای ادامه‌ی ثبت باز می‌شود. این کار کاملاً خودکار است و نیازی به دخالت شما ندارد؛ فقط با دستور `dellog` می‌توانید این آرشیوهای قدیمی را از حافظه‌ی گوشی پاک کنید.

### برداشتن فایل از گوشی به‌صورت دستی
اگر نخواستید صبر کنید تا `sendlog` ایمیل بفرستد، می‌توانید مستقیم فایل را از گوشی بردارید:
- با یک File Manager که دسترسی به پوشه‌ی `Android/data` را نشان می‌دهد (در اندروید ۱۱ به بعد بعضی File Managerها نیاز به فعال‌کردن دسترسی خاص دارند)
- یا با دستور `adb pull` از کامپیوتر، اگر Android Studio و درایورهای USB گوشی را نصب کرده‌اید:
```
adb pull /storage/emulated/0/Android/data/ir.example.locationsms/files/location_log.csv
```

## تنظیم ایمیل برای دستور `sendlog`
برای اینکه `sendlog` کار کند، اپ باید بتواند خودش (بدون باز کردن اپ ایمیل شما) وارد یک حساب ایمیل بشود و از طریق آن نامه بفرستد. توصیه می‌شود یک حساب Gmail جدید و جداگانه فقط برای همین کار بسازید (نه ایمیل شخصی اصلی‌تان) و مراحل زیر را انجام دهید:

1. وارد حساب Gmail (جدید یا موجود) بشوید و **تایید دومرحله‌ای (2-Step Verification)** را در تنظیمات امنیتی حساب فعال کنید — بدون این مرحله، گوگل اجازه‌ی ساخت App Password نمی‌دهد.
2. به آدرس `myaccount.google.com/apppasswords` بروید (یا از تنظیمات حساب گوگل، بخش Security، دنبال «App passwords» بگردید).
3. یک نام دلخواه (مثلاً `MyCarTracker`) وارد کنید و روی Create/Generate بزنید. یک رمز ۱۶ کاراکتری (بدون فاصله) به شما نشان داده می‌شود — این را کپی کنید.
4. داخل اپ My Car Tracker، در بخش «تنظیمات ارسال فایل لاگ به ایمیل»:
   - **ایمیل گیرنده:** آدرسی که می‌خواهید فایل لاگ به آن برسد (می‌تواند همان ایمیل شخصی شما باشد)
   - **ایمیل فرستنده:** همان حساب Gmail که در مرحله ۱ ساختید
   - **App Password ایمیل فرستنده:** همان رمز ۱۶ کاراکتری مرحله ۳ (نه رمز عبور اصلی جیمیل)
   - **آدرس سرور SMTP / پورت:** برای Gmail معمولاً نیازی به تغییر نیست (پیش‌فرض `smtp.gmail.com` و `587` درست است)
5. «ذخیره و درخواست مجوزها» را بزنید.

بعد از این تنظیم، هر وقت از یکی از شماره‌های مجاز `sendlog` بفرستید، فایل‌های لاگ به‌عنوان پیوست به همان ایمیل گیرنده ارسال می‌شوند.

## توسعه‌های پیشنهادی بعدی (اختیاری)
- اضافه‌کردن گزینه «تغییر رمز عبور» از داخل تنظیمات اپ.
- نمایش تاریخچه موقعیت‌های ذخیره‌شده (همان فایل CSV) در خود اپ، به‌جای نیاز به File Manager یا adb.
- قفل با اثر انگشت (BiometricPrompt) به‌جای/علاوه‌بر رمز عبور متنی.
- فشرده‌سازی فایل‌های لاگ در یک ZIP قبل از ارسال ایمیل، برای حجم کمتر.

## درباره
طراح: امید شاهین مهر
ایمیل: omidshahinmehr@gmail.com
